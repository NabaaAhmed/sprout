name = "sprout-quiz-worker"
main = "worker.js"
compatibility_date = "2025-10-01"
workers_dev = true

# Store your Gemini API key as a secret (never commit it):
#   npx wrangler secret put GEMINI_API_KEY
#
# Then deploy:
#   npx wrangler deploy
#
# Live URL:
#   https://sprout-quiz-worker.sprout-nabaa.workers.dev
